\* Specification name (generated by PlusCal)
SPECIFICATION Spec

\* Constant Definitions
CONSTANTS
    Proc = {p1, p2, p3, p4}
    Values = {v1}
    t = 1
    Byzantine = {p4}
    Initiator = p1

\* Safety Properties (Checked every state)
INVARIANTS
    BRB_Validity
    BRB_Integrity
    BRB_NoDuplicity

\* Liveness Properties (Checked over entire behaviors)
\* PROPERTIES
\*    BRB_Termination1
\*    BRB_Termination2

\* State Constraint to prevent infinite state space exploration
\* due to the Byzantine process's arbitrary message generation.
CONSTRAINT
    StateConstraint